[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA fallback so client routing / refresh works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
